{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist -- --legacy-peer-deps",
    "dev": "lerna run --parallel dev"
  },
  "dependencies": {
    "svgson": "^5.2.1"
  }
}
